{
  "id": "GOLDEN-003",
  "title": "Hardcoded API key in source code",
  "expected_finding": true,
  "severity": "P1",
  "category": "secrets",
  "code": "const apiKey = \"EXAMPLE_AWS_ACCESS_KEY\"\n\nfunc callAWS() {\n    client := aws.NewClient(apiKey)\n    client.ListBuckets()\n}",
  "finding_pattern": "hardcoded secret",
  "coverage_rules": [
    "No hardcoded secrets in source/config samples",
    "Secrets loaded from env/secret manager only",
    "Secret / Config Management",
    "fail-fast if empty",
    "No default values for secrets in code"
  ],
  "note": "Domain 4 is 'Secret / Config Management' in the canonical stack-independent set (authorization-and-policy.md §2); the per-stack evidence lives in go-secure-coding.md Domain 4 and the matching lang-*.md row.",
  "reference": "references/scenario-checklists.md"
}
