{
  "id": "GOLDEN-001",
  "title": "Oracle-mode auto-increment primary key -- must use IDENTITY / a sequence, AUTO_INCREMENT must not appear",
  "scenario": "Enterprise Edition, Oracle mode, V4.5.0; the user wants an auto-incrementing id column as the primary key.",
  "rules": [
    "R-AUTOINC-MODE-GATE"
  ],
  "coverage_rules": [
    "GENERATED BY DEFAULT AS IDENTITY",
    "CREATE SEQUENCE"
  ],
  "anti_example_patterns": [
    "AUTO_INCREMENT` family is exclusive to MySQL mode"
  ],
  "must_be_absent_from_oracle_bnf": [
    "AUTO_INCREMENT_MODE"
  ]
}
