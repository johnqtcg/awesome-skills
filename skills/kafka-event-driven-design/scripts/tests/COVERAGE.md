<!-- GENERATED by scripts/gen_coverage.py — do not edit by hand. -->
# kafka-event-driven-design — Test Coverage

## 1. Gate Summary — 8 gates

| Gate | What it proves | Size |
|------|----------------|------|
| Linter self-test (every rule fires and stays quiet) | every rule can fire AND stay quiet | 26 rules, 128 cases |
| Semantic lint of shipped documentation | shipped docs assert no known-wrong claim | 26 rules over 5 files |
| Mutation sweep (inverted claims must be caught) | an inverted claim is actually caught | 34 mutations |
| Paraphrase corpus (restatements the docs never use) | restatements the docs never use are still caught | 128 probes over 26 rules |
| Contract tests (structure + checkable claims) | required structure and checkable claims | 94 tests |
| Golden scenarios (detector expectations + verified facts) | detector expectations + verified upstream facts | 241 tests |
| Model-eval grader calibration (offline, no model needed) | the model-eval grader separates a correct review from a confident wrong one | 21 good/decoy pairs, margin ≥ 0.3 |
| Coverage doc freshness (generated, never hand-maintained) | this table matches the runner and the sources | derived, not hand-listed |

**Total: 335 pytest tests, 128 linter self-test cases, 128 paraphrase probes, 34 mutations.**

The gate rows are parsed from `scripts/run_regression.sh`, not listed here — a gate added to the runner shows up in this table or `--check` fails. Sizes are counted from the modules themselves; regenerate with `python3 scripts/gen_coverage.py`.

## 2. Semantic Rules

| Rule | Kind | Claim it defends | Fixture | Mutation |
|------|------|------------------|:-------:|:--------:|
| `KL001` | forbid | acks=1 / acks=0 recommended as the safe choice | — | yes |
| `KL002` | forbid | enable.idempotence disabled as a recommendation | — | yes |
| `KL003` | forbid | BACKWARD described as letting OLD readers read NEW data (that is FORWARD) | — | yes |
| `KL004` | forbid | BACKWARD or FULL claimed to forbid all field deletion | — | yes |
| `KL005` | forbid | BACKWARD_TRANSITIVE asserted as universally safest | — | yes |
| `KL006` | forbid | null key described as round-robin | — | yes |
| `KL007` | forbid | high key cardinality claimed to create too many partitions | — | yes |
| `KL008` | forbid | sharded/composite hot key claimed to preserve Kafka-native ordering | — | yes |
| `KL009` | forbid | hard-coded events/sec threshold for global ordering | — | yes |
| `KL010` | forbid | Kafka transaction claimed to cover a database/outbox write | yes | yes |
| `KL011` | forbid | outbox or DLQ described as exactly-once | — | yes |
| `KL012` | forbid | removed sendOffsetsToTransaction(String) overload | yes | yes |
| `KL013` | forbid | unconditional 'every consumer must have a DLQ' framing | — | yes |
| `KL014` | forbid | raw JSON declared categorically unacceptable in production | — | yes |
| `KL015` | forbid | RF=1 or low RF declared unacceptable without a trade-off path | — | yes |
| `KL016` | forbid | max.in.flight must be 1 stated as a general (non-sarama) rule | — | yes |
| `KL017` | forbid | Kafka version model stops before 4.x | — | yes |
| `KL018` | require | CooperativeStickyAssignor recommended without protocol qualification | yes | yes |
| `KL019` | require | exactly-once described without the consumer-side isolation level | yes | yes |
| `KL020` | block_require | cache-TTL deduplication presented as sufficient idempotency | yes | yes |
| `KL021` | require | DLQ publish result discarded before advancing the offset | yes | yes |
| `KL022` | block | group.protocol=consumer set alongside a config it disables | yes | yes |
| `KL023` | forbid | compaction recommended for an event-sourced log | yes | yes |
| `KL024` | forbid | fixed consumer-lag alert threshold presented as a recommendation | yes | yes |
| `KL025` | forbid | static membership claimed to eliminate rebalance on restart | yes | yes |
| `KL026` | forbid | Avro-vs-Protobuf speed stated as a fact rather than measured | — | yes |

Rules exercised by a mutation: **26/26**. Every rule is mutation-covered.

## 3. Golden Fixtures

| ID | Type | Severity | Detector rules | Title |
|----|------|----------|----------------|-------|
| KAFKA-001 | defect | critical | — (false-positive guard) | Producer with acks=1 for critical business events — data loss on leade |
| KAFKA-002 | defect | critical | — (false-positive guard) | Consumer without idempotency — duplicate delivery causes double proces |
| KAFKA-003 | defect | critical | — (false-positive guard) | No dead letter queue — poison message blocks entire partition |
| KAFKA-004 | defect | standard | — (false-positive guard) | Null partition key — events for same entity scattered across partition |
| KAFKA-005 | defect | standard | — (false-positive guard) | Event missing metadata — no event_id, timestamp, or source for debuggi |
| KAFKA-006 | defect | standard | — (false-positive guard) | Schema evolution without compatibility mode — breaking change deployed |
| KAFKA-007 | good_practice | none | — (false-positive guard) | Well-formed event-driven design — acks=all, idempotent consumer, DLQ,  |
| KAFKA-008 | good_practice | none | — (false-positive guard) | Good outbox pattern — transactional event publishing with DB atomicity |
| KAFKA-009 | degradation_scenario | none | — (false-positive guard) | Degraded mode — Kafka design review with no version or delivery requir |
| KAFKA-010 | workflow | none | — (false-positive guard) | Workflow — design event-driven order processing system from scratch |
| KAFKA-011 | defect | standard | — (false-positive guard) | No consumer lag monitoring — silent event processing delay undetected |
| KAFKA-012 | defect | critical | KL012, KL019 | Exactly-once pipeline using the sendOffsetsToTransaction overload remo |
| KAFKA-013 | defect | critical | KL021 | DLQ publish error discarded before the offset advances — poison messag |
| KAFKA-014 | defect | standard | KL018, KL022 | Kafka 4.x consumer protocol enabled alongside configs it disables |
| KAFKA-015 | defect | critical | KL010 | Database write placed inside a Kafka transaction — an atomicity bounda |
| KAFKA-016 | defect | critical | KL020 | Cache-TTL deduplication presented as the consumer idempotency mechanis |
| KAFKA-017 | good_practice | none | — (false-positive guard) | Modern Java client with no explicit acks — safe by default, must not b |
| KAFKA-018 | defect | standard | — (false-positive guard) | PII on a compacted, infinite-retention topic with no deletion path |
| KAFKA-019 | defect | critical | KL023 | Event-sourced log compacted by aggregate ID — replay reconstructs the  |
| KAFKA-020 | defect | standard | KL024 | Consumer lag alert copied as a raw message count with no freshness SLO |
| KAFKA-021 | defect | standard | KL025 | Static membership adopted as a blanket rebalance cure, with the defaul |

By type: defect 16, degradation_scenario 1, good_practice 3, workflow 1.
 By severity: critical 8, none 5, standard 8.

Fixtures with no detector rules are not filler — they assert the linter stays silent on realistic Kafka code, which is the false-positive half of the contract.

## 4. Known Gaps

| Gap | Priority | Why it is still open |
|-----|----------|----------------------|
| No live broker in the loop | High | Every claim here is verified against Apache Kafka source and official docs, not against a running 2.8/3.x/4.x cluster. A server matrix would catch facts that are true in source but not in practice. |
| No scored model run | High | `scripts/model_eval.py` runs the A/B (prompt alone vs prompt + SKILL.md) over the 16 defect fixtures with a deterministic grader, and its grader is calibrated offline by gate 7 — but **no arm has been scored against a real model**. A nested `claude -p` does not inherit session credentials, so it exits 2 (`SKIPPED (infra)`, never 0). Run it in an authenticated shell: `python3 scripts/model_eval.py --json ab.json`, or point `--runner` at any CLI. |
| Trigger precision unmeasured | High | `scripts/trigger_eval.py` (30 probes: 15 in-scope, 15 near-miss out-of-scope from §1's exclusion table) has produced **no scored run**, same credential reason. It is also only a proxy: it measures whether the frontmatter `description` is classifiable by a judge model, not whether the host agent fires. |
| Semantic lint is a known-regression detector, not a semantic checker | Medium | 26 regexes over English. A finding is real; silence is not a clearance. 128 paraphrase probes raise the floor, and four natural restatements still walked through a fully green suite in the most recent round. Coverage grows by adding paraphrases, never by trusting the regex to generalise. |
| Chinese-language claims unlinted | Medium | The rules are English regexes; the bilingual eval reports are checked by hand. |
| SKILL.md is always-loaded context | Medium | 33,088 bytes, 424 lines. The anti-example bodies and the tier checklists now live in `references/`; what remains is the gate/checklist/scorecard path itself, and further cuts trade away correctness qualifications rather than duplication. |
| Kafka Streams / Connect | Low | Out of scope per SKILL.md §1. |
