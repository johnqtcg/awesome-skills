SMOKE TEST MODE — stock-analysis-lead orchestration check.

Use the stock-analysis-lead skill to analyze ticker MOCK at Lite depth, with these overrides (smoke harness, not a real analysis):

1. MOCK is a fictional US-listed company. Skip Step 1 EDGAR validation — treat MOCK as a validated 10-K filer.
2. Skip Step 2 network fetching entirely. The data package is already prepared at __SCRATCH__ with manifest __SCRATCH__/data-manifest.json. Do not use WebSearch or WebFetch anywhere in this run, and instruct every dispatched worker likewise (offline mode; rely only on the manifest's local files).
3. Append the verdict log entry to __SCRATCH__/verdicts.jsonl instead of the default path.
4. Everything else runs exactly per the skill: classify the archetype, dispatch all 6 workers in one parallel batch, consolidate their Findings JSON blocks, synthesize the verdict, write the log entry.

Produce the full report in English.