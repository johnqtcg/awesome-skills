"""finlib — deterministic data + calculation layer for stock-analysis-lead.

Modules:
  ratios   — named financial-ratio functions (formula == function name)
  lint     — 口径 / consistency / single-period linter
  edgar    — SEC EDGAR XBRL first-hand data pipeline
  model    — driver-model schema + false-precision guardrail
  valuation— driver-based DCF / reverse-DCF / scenarios / sensitivity
  sotp     — sum-of-the-parts for option-dominated / multi-engine companies
"""