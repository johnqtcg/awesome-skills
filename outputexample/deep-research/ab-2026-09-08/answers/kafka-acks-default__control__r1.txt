Since Kafka 3.0 (via KIP-679), the Java producer's default `acks` setting changed from `1` to `all`, and this remains the default throughout the 3.x line. So in Kafka 3.x, the default is `all`, not `1`.

VERDICT: NO

SOURCES:
- https://cwiki.apache.org/confluence/display/KAFKA/KIP-679:+Producer+will+enable+the+strongest+delivery+guarantee+by+default ||| Producer config defaults will change in release version 3.0
- https://issues.apache.org/jira/browse/KAFKA-10619 ||| Enable producer idempotence by default (KIP-679)